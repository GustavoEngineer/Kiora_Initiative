const BlocModel = require('./blocModel');
const TagModel = require('./tagModel');
const TaskModel = require('./taskModel');
const WorkDayModel = require('./workDayModel');

module.exports = {
    BlocModel,
    TagModel,
    TaskModel,
    WorkDayModel,
};
